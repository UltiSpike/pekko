# Rush Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Rush Mode（第 5 个 preset Mode）以及 Custom Mode 的 `customArcadeEnabled` toggle——combo 驱动的打击层 overlay + 托盘图标 HUD，维持 Pekko 现有 <10ms 端到端延迟，对齐现有 hold-repeat (`playSound('repeat')`) 的 skip 策略。

**Architecture:** 两个入口（Rush preset / Custom toggle）共用同一引擎能力：`ArcadeOverlayLayer`（12 声部独立池 + 4 层打击鼓组 + PERFECT 判定 + combo 状态机）和 `ArcadeHudController`（托盘图标状态 IPC + rAF 节流）。Overlay 总线绕过主压缩器、每击 `source.start(playTime)` 与主音同调度，零感知延迟。`type === 'repeat'` 路径完全跳过 overlay 和 combo 逻辑。

**Tech Stack:** TypeScript + Electron + Web Audio API · 测试用 Node 18+ 内置 `node:test` + `tsx --test`（零新依赖）· 托盘图标 PNG 用 `@resvg/resvg-js`（已在 devDependencies）从 SVG 生成

**Design Spec:** `docs/superpowers/specs/2026-04-15-rush-mode-design.md`

---

## 文件结构

**新建目录**：`src/renderer/audio/arcade/`

**新建文件**：
- `src/renderer/audio/arcade/combo.ts` — 纯函数 combo 状态机（combo 计数 / reset / stage 映射 / PERFECT 判定）
- `src/renderer/audio/arcade/combo.test.ts` — `node:test` 单元测试
- `src/renderer/audio/arcade/synth/kick.ts` — 程序生成 kick AudioBuffer
- `src/renderer/audio/arcade/synth/hat.ts` — 程序生成 closed hat AudioBuffer
- `src/renderer/audio/arcade/synth/clap.ts` — 程序生成 clap AudioBuffer
- `src/renderer/audio/arcade/synth/swell.ts` — 程序生成 shimmer swell AudioBuffer
- `src/renderer/audio/arcade/synth/synth.test.ts` — 缓冲区形状单元测试
- `src/renderer/audio/arcade/ArcadeOverlayLayer.ts` — 引擎能力类
- `src/renderer/audio/arcade/ArcadeHudController.ts` — 渲染进程 HUD 状态机
- `scripts/gen-rush-icons.js` — 从 SVG 生成托盘 PNG 的脚本
- `assets/icons/tray-rush/rush-base.svg` — 非 template 激活态基础 SVG
- `assets/icons/tray-rush/rush-bright.svg` — 非 template 激活态高亮 SVG
- `assets/icons/tray-rush/rush-perfect.svg` — PERFECT 闪帧 SVG
- `assets/icons/tray-rush/*.png` — 生成产物（每 SVG 出 @1x @2x 两份）

**修改文件**：
- `src/shared/modes.ts` — `Mode` 加 `arcadeEnabled`、新增 `STYLE.rush`、新增 Rush Mode、扩 `buildCustomMode` 签名
- `src/shared/types.ts` — `AppSettings` 加 `customArcadeEnabled: boolean`
- `src/main/store.ts` — `defaults` 加默认值，`mergeSettings` 加 boolean 判断，`setCustomConfig` 扩签名
- `src/main/ipc-handlers.ts` — 注册 `update-arcade-hud` 的 `ipcMain.on` handler，扩 `set-custom-config` handler
- `src/main/preload.ts` — 暴露 `updateArcadeHud(state)` 到 api
- `src/main/tray.ts` — 新增 `updateArcadeHud(state)` 导出，管理 template ↔ non-template 切换和脉冲 interval
- `src/renderer/audio/AudioEngine.ts` — 实例化 `ArcadeOverlayLayer` + `ArcadeHudController`，在 `playSound('down')` 挂钩、`playSound('repeat')` 跳过、`setMode` 中 activate/deactivate
- `src/renderer/hooks/useAudioEngine.ts` — 透传 arcadeEnabled 到 AudioEngine
- `src/renderer/components/TuneView.tsx` — Custom Mode 加 "Arcade feedback" toggle 行
- `package.json` — 加 `test` script

---

## 任务列表

### Task 1: 测试基建 + Schema 扩展

**Files:**
- Modify: `package.json`（加 `test` script）
- Modify: `src/shared/modes.ts`（`Mode` 接口加 `arcadeEnabled`、新增 `STYLE.rush`、新增 Rush Mode、扩 `buildCustomMode`）
- Modify: `src/shared/types.ts`（`AppSettings` 加 `customArcadeEnabled`）
- Modify: `src/main/store.ts`（defaults + mergeSettings + setCustomConfig）

- [ ] **Step 1: 加 test script**

Edit `package.json`，在 `scripts` 对象中 `build` 之后加一行：

```json
"test": "tsx --test \"src/**/*.test.ts\"",
```

完整的 `scripts` 段：

```json
"scripts": {
  "dev": "npm run build:main && concurrently -k \"vite\" \"electron .\"",
  "dev:debug": "npm run build:main && concurrently -k \"vite\" \"DEVTOOLS=1 electron .\"",
  "dev:renderer": "vite",
  "build:renderer": "vite build",
  "build:main": "tsc -p tsconfig.main.json",
  "build": "npm run build:renderer && npm run build:main",
  "test": "tsx --test \"src/**/*.test.ts\"",
  "package": "npm run build && electron-builder",
  "postinstall": "electron-builder install-app-deps"
}
```

- [ ] **Step 2: 验证 test 命令能跑（空测试）**

Run: `npm test`
Expected: 命令退出码 0，output 显示 0 tests run（因为还没写测试文件）

- [ ] **Step 3: 扩 `Mode` 接口 + 加 `STYLE.rush`**

Edit `src/shared/modes.ts`：

找到 `export interface Mode { ... }`，在 `style: ModeStyle` 之后加一行：

```ts
export interface Mode {
  id: string
  name: string
  subtitle: string
  description: string
  bed: BedType
  bedGainDb: number
  style: ModeStyle
  arcadeEnabled: boolean  // Rush preset + Custom toggle
}
```

在 `STYLE` 常量中 `deep` 之后加 `rush`：

```ts
const STYLE: Record<StyleLevel, ModeStyle> = {
  minimal: { /* ... 现有 */ },
  soft:    { /* ... 现有 */ },
  full:    { /* ... 现有 */ },
  deep:    { /* ... 现有 */ },
  rush: {
    pitchJitter: 0.010,
    volumeJitter: 0.05,
    intensityLocked: true,
    airLpfHz: 14000,
    highShelfDb: 1.5,
    lowShelfDb: 1.5,
    wetMix: 0.10,
  },
}
```

注意：`StyleLevel` 类型定义在顶部，也要扩：

```ts
export type StyleLevel = 'minimal' | 'soft' | 'full' | 'deep' | 'rush'
```

- [ ] **Step 4: 新增 Rush Mode 到 `MODES` 数组，扩现有 4 个 Mode 加 `arcadeEnabled: false`**

Edit `src/shared/modes.ts` 的 `MODES` 数组。**每一个**现有 Mode 字面量都要加 `arcadeEnabled: false`（否则 TS 会报缺字段）：

```ts
export const MODES: Mode[] = [
  {
    id: 'deep-focus',
    name: 'Deep Focus',
    subtitle: 'Brown bed · muted keys',
    description: 'Warm brown-noise bed and muted keys. Disappear into the work.',
    bed: 'brown',
    bedGainDb: -38,
    style: STYLE.minimal,
    arcadeEnabled: false,
  },
  {
    id: 'cozy-writing',
    name: 'Cozy Writing',
    subtitle: 'Pink bed · soft keys',
    description: 'Pink-noise warmth, softer keys. Good company for long sessions.',
    bed: 'pink',
    bedGainDb: -40,
    style: STYLE.soft,
    arcadeEnabled: false,
  },
  {
    id: 'thock',
    name: 'Thock',
    subtitle: 'Deep, roomy · no bed',
    description: 'Heavy low-end, warm decay. Satisfying weight on every keystroke.',
    bed: 'none',
    bedGainDb: -80,
    style: STYLE.deep,
    arcadeEnabled: false,
  },
  {
    id: 'classic-mech',
    name: 'Classic Mech',
    subtitle: 'No bed · full fidelity',
    description: 'No bed, full mechanical fidelity. Every switch as recorded.',
    bed: 'none',
    bedGainDb: -80,
    style: STYLE.full,
    arcadeEnabled: false,
  },
  {
    id: 'rush',
    name: 'Rush',
    subtitle: 'Combo overlay · momentum HUD',
    description: 'Percussive hits stack with combo, pulse tracks the flow. Built for sprints, not marathons.',
    bed: 'none',
    bedGainDb: -80,
    style: STYLE.rush,
    arcadeEnabled: true,
  },
]
```

- [ ] **Step 5: 扩 `buildCustomMode` 签名**

Edit `src/shared/modes.ts` 的 `buildCustomMode`：

```ts
export function buildCustomMode(
  style: ModeStyle,
  bed: BedType,
  bedGainDb: number,
  arcadeEnabled: boolean,
): Mode {
  return {
    id: 'custom',
    name: 'Custom',
    subtitle: 'Your engine',
    description: 'Your own blend — tune bed, EQ, and jitter to taste.',
    bed,
    bedGainDb,
    style,
    arcadeEnabled,
  }
}
```

- [ ] **Step 6: 扩 `AppSettings`**

Edit `src/shared/types.ts`。在 `AppSettings` 接口中 `switchDspOverrides` 之前加一行：

```ts
export interface AppSettings {
  // ... 其他现有字段
  customBed: BedType
  customBedGainDb: number
  customStyle: ModeStyle
  customArcadeEnabled: boolean  // opt-in arcade overlay inside Custom Mode
  switchDspOverrides: Record<string, SwitchDspOverride>
}
```

- [ ] **Step 7: 扩 `store.ts` defaults + mergeSettings + setCustomConfig**

Edit `src/main/store.ts`：

在 `defaults` 常量中加一行：

```ts
const defaults: AppSettings = {
  activeProfile: 'cherrymx-black-abs',
  volume: 0.7,
  startAtLogin: false,
  mode: DEFAULT_MODE_ID,
  isTuning: false,
  finish: 'indigo-linen',
  uiSounds: false,
  holdRepeat: false,
  customBed: DEFAULT_CUSTOM_BED,
  customBedGainDb: DEFAULT_CUSTOM_BED_GAIN_DB,
  customStyle: { ...DEFAULT_CUSTOM_STYLE },
  customArcadeEnabled: false,
  switchDspOverrides: {},
}
```

在 `mergeSettings` 中加 boolean 强类型判断（跟 `holdRepeat` 同位置）：

```ts
return {
  ...defaults,
  ...stored,
  finish: finish ?? defaults.finish,
  holdRepeat: typeof stored.holdRepeat === 'boolean' ? stored.holdRepeat : defaults.holdRepeat,
  customArcadeEnabled: typeof stored.customArcadeEnabled === 'boolean' ? stored.customArcadeEnabled : defaults.customArcadeEnabled,
  customStyle: { ...defaults.customStyle, ...(stored.customStyle ?? {}) },
  switchDspOverrides: { ...defaults.switchDspOverrides, ...(stored.switchDspOverrides ?? {}) },
}
```

扩 `setCustomConfig` 签名以接收 arcade toggle：

```ts
export function setCustomConfig(cfg: { bed: BedType; bedGainDb: number; style: ModeStyle; arcadeEnabled: boolean }): void {
  const s = readStore()
  s.customBed = cfg.bed
  s.customBedGainDb = cfg.bedGainDb
  s.customStyle = cfg.style
  s.customArcadeEnabled = cfg.arcadeEnabled
  writeStore(s)
}
```

- [ ] **Step 8: 扩 `set-custom-config` IPC handler**

Edit `src/main/ipc-handlers.ts`。找到 `ipcMain.handle('set-custom-config', ...)` 行，更新 payload 类型：

```ts
ipcMain.handle('set-custom-config', (_event, cfg: { bed: BedType; bedGainDb: number; style: ModeStyle; arcadeEnabled: boolean }) => {
  setCustomConfig(cfg)
  return true
})
```

- [ ] **Step 9: 类型编译通过**

Run: `npm run build:main`
Expected: 无 TS 错误。

Run: `npm run build:renderer`
Expected: 无 TS 错误（可能出现警告说 `buildCustomMode` 调用点签名不匹配——如果是这种情况，下一步在 Task 8 的 `useAudioEngine.ts` / `TuneView.tsx` 调用点一起修）。

如果 `buildCustomMode` 旧调用点报错，本地快速修（搜索 `buildCustomMode(`）：

```bash
grep -rn "buildCustomMode(" src/
```

在每个调用点末尾加 `, settings.customArcadeEnabled ?? false`（或临时 `, false`，Task 8 会接入真实值）。

- [ ] **Step 10: Commit**

```bash
git add package.json src/shared/modes.ts src/shared/types.ts src/main/store.ts src/main/ipc-handlers.ts src/renderer/hooks/useAudioEngine.ts src/renderer/components/TuneView.tsx
git commit -m "feat(rush): Mode + AppSettings schema 扩展 · STYLE.rush + Rush preset + customArcadeEnabled"
```

---

### Task 2: Combo 状态机（TDD）

**Files:**
- Create: `src/renderer/audio/arcade/combo.ts`
- Test: `src/renderer/audio/arcade/combo.test.ts`

Combo 状态机是整个 Rush 的核心纯逻辑层。用 TDD 保证阈值、reset、PERFECT 判定、hold-repeat skip 这几条规则无误。

- [ ] **Step 1: 写 combo.test.ts（所有测试）**

Create `src/renderer/audio/arcade/combo.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
import {
  createComboState,
  recordKeydown,
  recordRepeat,
  getStage,
  ComboStage,
} from './combo.ts'

test('初始 combo 为 0，stage 为 idle', () => {
  const s = createComboState()
  assert.equal(s.combo, 0)
  assert.equal(getStage(s.combo), 'idle')
})

test('connected keydown 每击 combo +1', () => {
  const s = createComboState()
  recordKeydown(s, 1000)
  recordKeydown(s, 1100)
  recordKeydown(s, 1200)
  assert.equal(s.combo, 3)
})

test('inter-key 间隔 > 600ms 触发 reset', () => {
  const s = createComboState()
  recordKeydown(s, 1000)
  recordKeydown(s, 1100)
  recordKeydown(s, 2000)  // 900ms 间隔
  assert.equal(s.combo, 1)  // reset 到 0 然后 +1
})

test('边界：间隔恰好 600ms 不 reset', () => {
  const s = createComboState()
  recordKeydown(s, 1000)
  recordKeydown(s, 1600)
  assert.equal(s.combo, 2)
})

test('stage 阈值正确', () => {
  assert.equal(getStage(0), 'idle')
  assert.equal(getStage(1), 'idle')
  assert.equal(getStage(2), 'engaged')
  assert.equal(getStage(9), 'engaged')
  assert.equal(getStage(10), 'stacking')
  assert.equal(getStage(24), 'stacking')
  assert.equal(getStage(25), 'flow')
  assert.equal(getStage(49), 'flow')
  assert.equal(getStage(50), 'zone')
  assert.equal(getStage(999), 'zone')
})

test('hold-repeat 事件对 combo 完全不可见', () => {
  const s = createComboState()
  recordKeydown(s, 1000)
  recordKeydown(s, 1100)
  // 连按 5 次 repeat，combo 不动
  recordRepeat(s, 1200)
  recordRepeat(s, 1300)
  recordRepeat(s, 1400)
  recordRepeat(s, 1500)
  recordRepeat(s, 1600)
  assert.equal(s.combo, 2)
  assert.equal(s.lastKeydownMs, 1100)  // 没被 repeat 更新
})

test('hold-repeat 期间不影响 inter-key 计时——下一个 keydown 间隔从上一个 keydown 算起', () => {
  const s = createComboState()
  recordKeydown(s, 1000)
  recordRepeat(s, 1500)  // 500ms 后 repeat（不应更新 lastKeydownMs）
  recordRepeat(s, 2000)  // 1000ms 后又 repeat
  recordKeydown(s, 2200) // 距离上一 keydown(1000) = 1200ms > 600ms，reset
  assert.equal(s.combo, 1)
})

test('PERFECT 判定：最近 8 键间隔方差 / 均值 < 0.15 返回 true', () => {
  const s = createComboState()
  // 构造稳定节奏：每 120ms 一击，共 9 击（从而有 8 个间隔）
  for (let i = 0; i < 9; i++) recordKeydown(s, 1000 + i * 120)
  assert.equal(s.combo, 9)
  // σ/μ 应该是 0（完全均匀）
  const res = checkPerfect(s, 1000 + 8 * 120)
  assert.equal(res, true)
})

test('PERFECT 判定：节奏不稳（σ/μ > 0.15）返回 false', () => {
  const s = createComboState()
  // 构造忽快忽慢：50, 200, 50, 200, 50, 200, 50, 200
  const intervals = [50, 200, 50, 200, 50, 200, 50, 200]
  let t = 1000
  recordKeydown(s, t)
  for (const iv of intervals) {
    t += iv
    recordKeydown(s, t)
  }
  const res = checkPerfect(s, t)
  assert.equal(res, false)
})

test('PERFECT 不足 8 键窗口时返回 false', () => {
  const s = createComboState()
  for (let i = 0; i < 7; i++) recordKeydown(s, 1000 + i * 120)
  const res = checkPerfect(s, 1000 + 6 * 120)
  assert.equal(res, false)
})

test('PERFECT 冷却：触发后 2 秒内再次调用返回 false', () => {
  const s = createComboState()
  for (let i = 0; i < 9; i++) recordKeydown(s, 1000 + i * 120)
  const first = checkPerfect(s, 1960)
  assert.equal(first, true)
  // 1 秒后再试，应该还在冷却
  for (let i = 0; i < 9; i++) recordKeydown(s, 2960 + i * 120)
  const second = checkPerfect(s, 2960 + 8 * 120)  // 距首次 <2s
  assert.equal(second, false)
})

test('PERFECT 冷却：2 秒后可再次触发', () => {
  const s = createComboState()
  for (let i = 0; i < 9; i++) recordKeydown(s, 1000 + i * 120)
  checkPerfect(s, 1960)  // 首次触发
  // 3 秒后重建稳定节奏
  for (let i = 0; i < 9; i++) recordKeydown(s, 5000 + i * 120)
  const res = checkPerfect(s, 5000 + 8 * 120)
  assert.equal(res, true)
})
```

注意测试还 import 了 `checkPerfect` 但我没在 top import——补：

```ts
import {
  createComboState,
  recordKeydown,
  recordRepeat,
  checkPerfect,
  getStage,
  ComboStage,
} from './combo.ts'
```

- [ ] **Step 2: 运行测试，确认全部失败**

Run: `npm test`
Expected: FAIL，错误是 `Cannot find module './combo.ts'` 或类似。

- [ ] **Step 3: 实现 combo.ts**

Create `src/renderer/audio/arcade/combo.ts`：

```ts
export type ComboStage = 'idle' | 'engaged' | 'stacking' | 'flow' | 'zone'

export interface ComboState {
  combo: number
  lastKeydownMs: number
  /** 最近若干次 keydown 的时间戳，rolling window 用于 PERFECT 方差判定 */
  recentKeydownMs: number[]
  /** 上一次 PERFECT 触发时间戳，用于 2s 冷却 */
  lastPerfectMs: number
}

export const COMBO_RESET_INTERVAL_MS = 600
export const PERFECT_WINDOW_SIZE = 8
export const PERFECT_THRESHOLD = 0.15
export const PERFECT_COOLDOWN_MS = 2000

const STAGE_THRESHOLDS = {
  engaged: 2,
  stacking: 10,
  flow: 25,
  zone: 50,
} as const

export function createComboState(): ComboState {
  return {
    combo: 0,
    lastKeydownMs: 0,
    recentKeydownMs: [],
    lastPerfectMs: -Infinity,
  }
}

export function recordKeydown(s: ComboState, nowMs: number): void {
  const interval = s.lastKeydownMs === 0 ? 0 : nowMs - s.lastKeydownMs
  if (interval > COMBO_RESET_INTERVAL_MS) {
    s.combo = 0
    s.recentKeydownMs = []
  }
  s.combo += 1
  s.lastKeydownMs = nowMs
  s.recentKeydownMs.push(nowMs)
  // 只保留最近 PERFECT_WINDOW_SIZE + 1 个时间戳（+1 是因为 8 个间隔需要 9 个点）
  const keep = PERFECT_WINDOW_SIZE + 1
  if (s.recentKeydownMs.length > keep) {
    s.recentKeydownMs.splice(0, s.recentKeydownMs.length - keep)
  }
}

/**
 * Hold-repeat 事件完全不动 combo 状态。
 * 保留此函数作为显式 API，调用点传 'repeat' 时调这个。
 */
export function recordRepeat(_s: ComboState, _nowMs: number): void {
  // no-op，显式记录 hold-repeat 不影响 combo
}

/**
 * 基于当前 recentKeydownMs window 判断是否达 PERFECT。
 * 触发后 2s 冷却。
 */
export function checkPerfect(s: ComboState, nowMs: number): boolean {
  if (s.recentKeydownMs.length < PERFECT_WINDOW_SIZE + 1) return false
  if (nowMs - s.lastPerfectMs < PERFECT_COOLDOWN_MS) return false

  const ts = s.recentKeydownMs.slice(-PERFECT_WINDOW_SIZE - 1)
  const intervals: number[] = []
  for (let i = 1; i < ts.length; i++) intervals.push(ts[i] - ts[i - 1])

  const mean = intervals.reduce((a, b) => a + b, 0) / intervals.length
  if (mean === 0) return false
  const variance = intervals.reduce((a, b) => a + (b - mean) ** 2, 0) / intervals.length
  const stddev = Math.sqrt(variance)

  if (stddev / mean < PERFECT_THRESHOLD) {
    s.lastPerfectMs = nowMs
    return true
  }
  return false
}

export function getStage(combo: number): ComboStage {
  if (combo >= STAGE_THRESHOLDS.zone) return 'zone'
  if (combo >= STAGE_THRESHOLDS.flow) return 'flow'
  if (combo >= STAGE_THRESHOLDS.stacking) return 'stacking'
  if (combo >= STAGE_THRESHOLDS.engaged) return 'engaged'
  return 'idle'
}

/**
 * Rush / Custom+arcade 切出时清状态。
 */
export function resetComboState(s: ComboState): void {
  s.combo = 0
  s.lastKeydownMs = 0
  s.recentKeydownMs = []
  s.lastPerfectMs = -Infinity
}
```

- [ ] **Step 4: 运行测试，全部通过**

Run: `npm test`
Expected: All PASS（~11 tests）。

如果有 fail，根据错误信息定位（常见是 window size 边界差 1、或 variance 算成了有偏估计）。

- [ ] **Step 5: Commit**

```bash
git add src/renderer/audio/arcade/combo.ts src/renderer/audio/arcade/combo.test.ts
git commit -m "feat(rush): combo 状态机 + PERFECT 判定 + TDD 单元测试"
```

---

### Task 3: 合成采样生成器

**Files:**
- Create: `src/renderer/audio/arcade/synth/kick.ts`
- Create: `src/renderer/audio/arcade/synth/hat.ts`
- Create: `src/renderer/audio/arcade/synth/clap.ts`
- Create: `src/renderer/audio/arcade/synth/swell.ts`
- Test: `src/renderer/audio/arcade/synth/synth.test.ts`

**对 spec §8 的说明**：spec 原计划 clap + swell 走 CC0 采样。为让 plan 自包含、避免外部资源依赖，v1 全部采用 Web Audio 合成实现。合成音色足够可听，需要更"人声感"的 clap 可在 v1 后以单独 PR 替换 `samples/` 目录。函数签名保持一致，替换零成本。

每个函数签名：`generateX(ctx: OfflineAudioContext | BaseAudioContext): Promise<AudioBuffer>`——用 OfflineAudioContext 离线渲染确保长度固定。

- [ ] **Step 1: 写 synth.test.ts（缓冲区形状核查）**

Create `src/renderer/audio/arcade/synth/synth.test.ts`：

```ts
import { test } from 'node:test'
import assert from 'node:assert/strict'
// 需要 Node 内置的 OfflineAudioContext polyfill，但 node 本身没有 Web Audio。
// 用极简 mock 代替用于形状测试：我们只测返回值是 "AudioBuffer-like"（length 符合预期）。
//
// 真实 AudioBuffer 构建依赖 Web Audio 运行时。这里我们只测 factory 产生的
// 配置参数是否正确（通过 inspectable 签名暴露）。

import { KICK_DURATION_S, HAT_DURATION_S, CLAP_DURATION_S, SWELL_DURATION_S } from './constants.ts'

test('KICK_DURATION_S ≈ 0.12s', () => {
  assert.ok(KICK_DURATION_S >= 0.10 && KICK_DURATION_S <= 0.15)
})

test('HAT_DURATION_S ≈ 0.04s', () => {
  assert.ok(HAT_DURATION_S >= 0.03 && HAT_DURATION_S <= 0.06)
})

test('CLAP_DURATION_S ≈ 0.08s', () => {
  assert.ok(CLAP_DURATION_S >= 0.06 && CLAP_DURATION_S <= 0.10)
})

test('SWELL_DURATION_S ≥ 1s（loop 基底）', () => {
  assert.ok(SWELL_DURATION_S >= 1.0)
})
```

- [ ] **Step 2: 运行测试，失败（找不到 constants.ts）**

Run: `npm test`
Expected: FAIL，`Cannot find module './constants.ts'`

- [ ] **Step 3: 创建 constants.ts**

Create `src/renderer/audio/arcade/synth/constants.ts`：

```ts
export const KICK_DURATION_S = 0.12
export const HAT_DURATION_S = 0.04
export const CLAP_DURATION_S = 0.08
export const SWELL_DURATION_S = 2.0
export const SAMPLE_RATE = 44100
```

- [ ] **Step 4: 运行测试，通过**

Run: `npm test`
Expected: PASS（新增 4 个 synth 测试 + Task 2 的 11 个 combo 测试）。

- [ ] **Step 5: 实现 kick.ts**

Create `src/renderer/audio/arcade/synth/kick.ts`：

```ts
import { KICK_DURATION_S, SAMPLE_RATE } from './constants.ts'

/**
 * 60 → 45 Hz 下滑的软 kick，无 click。
 * Exponential amp envelope 10ms attack / 110ms decay。
 */
export async function generateKick(): Promise<AudioBuffer> {
  const offline = new OfflineAudioContext(1, Math.ceil(KICK_DURATION_S * SAMPLE_RATE), SAMPLE_RATE)
  const osc = offline.createOscillator()
  const gain = offline.createGain()

  osc.type = 'sine'
  osc.frequency.setValueAtTime(60, 0)
  osc.frequency.exponentialRampToValueAtTime(45, KICK_DURATION_S)

  gain.gain.setValueAtTime(0, 0)
  gain.gain.linearRampToValueAtTime(1.0, 0.005)        // 5ms attack（去 click）
  gain.gain.exponentialRampToValueAtTime(0.0001, KICK_DURATION_S)

  osc.connect(gain)
  gain.connect(offline.destination)

  osc.start(0)
  osc.stop(KICK_DURATION_S)

  return offline.startRendering()
}
```

- [ ] **Step 6: 实现 hat.ts**

Create `src/renderer/audio/arcade/synth/hat.ts`：

```ts
import { HAT_DURATION_S, SAMPLE_RATE } from './constants.ts'

/**
 * Filtered white noise + fast decay envelope。闭镲感。
 * 8kHz HPF + 12kHz LPF 窄带通。
 */
export async function generateHat(): Promise<AudioBuffer> {
  const offline = new OfflineAudioContext(1, Math.ceil(HAT_DURATION_S * SAMPLE_RATE), SAMPLE_RATE)

  // White noise source
  const bufferSize = Math.ceil(HAT_DURATION_S * SAMPLE_RATE)
  const noiseBuf = offline.createBuffer(1, bufferSize, SAMPLE_RATE)
  const data = noiseBuf.getChannelData(0)
  for (let i = 0; i < bufferSize; i++) data[i] = Math.random() * 2 - 1

  const src = offline.createBufferSource()
  src.buffer = noiseBuf

  const hpf = offline.createBiquadFilter()
  hpf.type = 'highpass'
  hpf.frequency.value = 8000

  const lpf = offline.createBiquadFilter()
  lpf.type = 'lowpass'
  lpf.frequency.value = 12000

  const gain = offline.createGain()
  gain.gain.setValueAtTime(1.0, 0)
  gain.gain.exponentialRampToValueAtTime(0.0001, HAT_DURATION_S)

  src.connect(hpf).connect(lpf).connect(gain).connect(offline.destination)
  src.start(0)

  return offline.startRendering()
}
```

- [ ] **Step 7: 实现 clap.ts**

Create `src/renderer/audio/arcade/synth/clap.ts`：

```ts
import { CLAP_DURATION_S, SAMPLE_RATE } from './constants.ts'

/**
 * 三层相隔 ~12ms 的短噪声 burst 模拟 hand clap 的 "Da-da-DA" 触感。
 * 1.5–2.5 kHz 带通聚焦人声手感。
 */
export async function generateClap(): Promise<AudioBuffer> {
  const offline = new OfflineAudioContext(1, Math.ceil(CLAP_DURATION_S * SAMPLE_RATE), SAMPLE_RATE)

  // Noise source
  const bufferSize = Math.ceil(CLAP_DURATION_S * SAMPLE_RATE)
  const noiseBuf = offline.createBuffer(1, bufferSize, SAMPLE_RATE)
  const data = noiseBuf.getChannelData(0)
  for (let i = 0; i < bufferSize; i++) data[i] = Math.random() * 2 - 1

  const src = offline.createBufferSource()
  src.buffer = noiseBuf

  const bpf = offline.createBiquadFilter()
  bpf.type = 'bandpass'
  bpf.frequency.value = 2000
  bpf.Q.value = 1.2

  const gain = offline.createGain()
  // 模拟 "DA-da-DA" 三个 burst：0ms, 12ms, 24ms，每个 burst 12ms 衰减
  gain.gain.setValueAtTime(0, 0)
  const bursts = [0, 0.012, 0.024]
  for (const t of bursts) {
    gain.gain.setValueAtTime(0, t)
    gain.gain.linearRampToValueAtTime(1.0, t + 0.002)
    gain.gain.exponentialRampToValueAtTime(0.01, t + 0.012)
  }
  // 主 tail 从最后一个 burst 起
  gain.gain.exponentialRampToValueAtTime(0.0001, CLAP_DURATION_S)

  src.connect(bpf).connect(gain).connect(offline.destination)
  src.start(0)

  return offline.startRendering()
}
```

- [ ] **Step 8: 实现 swell.ts**

Create `src/renderer/audio/arcade/synth/swell.ts`：

```ts
import { SWELL_DURATION_S, SAMPLE_RATE } from './constants.ts'

/**
 * 宽频闪光 pad。多倍频程叠加的 filtered noise，慢 LFO 调制 LPF cutoff 产生闪光感。
 * 设计为 loop-safe：首尾衔接平滑。
 */
export async function generateSwell(): Promise<AudioBuffer> {
  const offline = new OfflineAudioContext(1, Math.ceil(SWELL_DURATION_S * SAMPLE_RATE), SAMPLE_RATE)

  // Dense noise
  const bufferSize = Math.ceil(SWELL_DURATION_S * SAMPLE_RATE)
  const noiseBuf = offline.createBuffer(1, bufferSize, SAMPLE_RATE)
  const data = noiseBuf.getChannelData(0)
  for (let i = 0; i < bufferSize; i++) data[i] = Math.random() * 2 - 1

  const src = offline.createBufferSource()
  src.buffer = noiseBuf

  // Bandpass 4-6 kHz 空气层
  const bpf = offline.createBiquadFilter()
  bpf.type = 'bandpass'
  bpf.frequency.value = 5000
  bpf.Q.value = 1.5

  // LFO 调制 bpf frequency 产生闪光感
  const lfo = offline.createOscillator()
  lfo.type = 'sine'
  lfo.frequency.value = 0.6  // 0.6 Hz 慢呼吸
  const lfoGain = offline.createGain()
  lfoGain.gain.value = 800   // 调制深度 ±800Hz
  lfo.connect(lfoGain).connect(bpf.frequency)

  // 整体 gain 保持恒定（loop 音色），不在 offline 渲染时加 ADSR——caller 在运行时做淡入淡出
  const gain = offline.createGain()
  gain.gain.value = 0.5

  src.connect(bpf).connect(gain).connect(offline.destination)
  src.start(0)
  lfo.start(0)

  return offline.startRendering()
}
```

- [ ] **Step 9: 手动 smoke test 合成器（可选但推荐）**

这部分没有自动测试——因为 Node `node:test` 没有 Web Audio 运行时。运行时 smoke test 在 Task 5 的 AudioEngine 集成时做。目前先确认 TS 编译通过。

Run: `npm run build:renderer`
Expected: 无 TS 错误。

- [ ] **Step 10: Commit**

```bash
git add src/renderer/audio/arcade/synth/
git commit -m "feat(rush): Web Audio 合成采样生成器（kick/hat/clap/swell）+ duration 常量测试"
```

---

### Task 4: 托盘图标 SVG + PNG 生成脚本

**Files:**
- Create: `assets/icons/tray-rush/rush-base.svg`
- Create: `assets/icons/tray-rush/rush-bright.svg`
- Create: `assets/icons/tray-rush/rush-perfect.svg`
- Create: `scripts/gen-rush-icons.js`
- Create: `assets/icons/tray-rush/*.png`（脚本产物）

- [ ] **Step 1: 写 rush-base.svg**

Create `assets/icons/tray-rush/rush-base.svg`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<svg width="44" height="44" viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg">
  <!-- warm amber fill，非 template，用 #E8A84C 在 light + dark 菜单栏都有辨识度 -->
  <circle cx="22" cy="22" r="14" fill="#E8A84C" opacity="0.7"/>
  <circle cx="22" cy="22" r="10" fill="#E8A84C" opacity="0.9"/>
  <circle cx="22" cy="22" r="5" fill="#FFFFFF" opacity="0.85"/>
</svg>
```

- [ ] **Step 2: 写 rush-bright.svg**

Create `assets/icons/tray-rush/rush-bright.svg`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<svg width="44" height="44" viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg">
  <circle cx="22" cy="22" r="15" fill="#FFC06A" opacity="0.85"/>
  <circle cx="22" cy="22" r="11" fill="#FFD080" opacity="0.95"/>
  <circle cx="22" cy="22" r="5" fill="#FFFFFF" opacity="1.0"/>
</svg>
```

- [ ] **Step 3: 写 rush-perfect.svg**

Create `assets/icons/tray-rush/rush-perfect.svg`：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<svg width="44" height="44" viewBox="0 0 44 44" xmlns="http://www.w3.org/2000/svg">
  <!-- 饱和度 + 亮度最大化的 PERFECT 一帧 -->
  <circle cx="22" cy="22" r="16" fill="#FFFFFF" opacity="0.4"/>
  <circle cx="22" cy="22" r="14" fill="#FFD080"/>
  <circle cx="22" cy="22" r="10" fill="#FFFFFF"/>
  <circle cx="22" cy="22" r="5" fill="#FFD080"/>
</svg>
```

- [ ] **Step 4: 写 gen-rush-icons.js**

Create `scripts/gen-rush-icons.js`：

```js
// 从 SVG 生成 @1x (22x22) 和 @2x (44x44) PNG，输出到 assets/icons/tray-rush/。
// macOS 托盘 template 标准是 18pt，我们用 22pt 给 amber fill 留边距。
const fs = require('fs')
const path = require('path')
const { Resvg } = require('@resvg/resvg-js')

const ROOT = path.join(__dirname, '..')
const ICON_DIR = path.join(ROOT, 'assets', 'icons', 'tray-rush')

const SVGS = ['rush-base', 'rush-bright', 'rush-perfect']
const SIZES = [
  { scale: 1, suffix: '' },      // 22x22
  { scale: 2, suffix: '@2x' },   // 44x44
]

function render(svgName) {
  const svgPath = path.join(ICON_DIR, `${svgName}.svg`)
  const svg = fs.readFileSync(svgPath)

  for (const { scale, suffix } of SIZES) {
    const width = 22 * scale
    const resvg = new Resvg(svg, { fitTo: { mode: 'width', value: width } })
    const png = resvg.render().asPng()
    const out = path.join(ICON_DIR, `${svgName}${suffix}.png`)
    fs.writeFileSync(out, png)
    console.log(`[rush-icons] ${out} (${width}x${width})`)
  }
}

for (const name of SVGS) render(name)
```

- [ ] **Step 5: 运行脚本生成 PNG**

Run: `node scripts/gen-rush-icons.js`
Expected: console 输出 6 行 `[rush-icons] .../rush-X.png (22x22 / 44x44)`，并生成 6 个 PNG 文件。

验证：`ls assets/icons/tray-rush/`
Expected: 3 个 SVG + 6 个 PNG。

- [ ] **Step 6: 加一个 script 别名到 package.json（方便后续重跑）**

Edit `package.json` 的 `scripts`：

```json
"gen:rush-icons": "node scripts/gen-rush-icons.js",
```

- [ ] **Step 7: Commit**

```bash
git add assets/icons/tray-rush/ scripts/gen-rush-icons.js package.json
git commit -m "feat(rush): 托盘图标 SVG 源 + 生成脚本 + @1x/@2x PNG"
```

---

### Task 5: ArcadeOverlayLayer（音频管线）

**Files:**
- Create: `src/renderer/audio/arcade/ArcadeOverlayLayer.ts`

整个 overlay 的音频侧：独立 12 声部池、4 层打击 + shimmer swell、overlay 总线绕过主压缩器、与 combo 模块联动。

- [ ] **Step 1: 创建 ArcadeOverlayLayer.ts 骨架**

Create `src/renderer/audio/arcade/ArcadeOverlayLayer.ts`：

```ts
import { generateKick } from './synth/kick.ts'
import { generateHat } from './synth/hat.ts'
import { generateClap } from './synth/clap.ts'
import { generateSwell } from './synth/swell.ts'
import {
  ComboState,
  createComboState,
  recordKeydown,
  checkPerfect,
  getStage,
  resetComboState,
  ComboStage,
} from './combo.ts'

const OVERLAY_POOL_SIZE = 12
const OVERLAY_MASTER_GAIN_DB = -6        // 相对主音
const LAYER_GAIN_DB = {
  kick:  -15,
  clap:  -12,
  hat:   -18,
  swell: -20,
}
const LAYER_FADE_IN_MS = {
  kick:  300,
  clap:  300,
  hat:   400,
  swell: 1000,
}
const SWELL_FADE_OUT_MS = 500

export interface OverlayCallbacks {
  onStageChange?: (stage: ComboStage, combo: number) => void
  onPerfect?: () => void
  onReset?: () => void
}

interface OverlayVoice {
  gain: GainNode
  source: AudioBufferSourceNode | null
  busy: boolean
}

function dbToGain(db: number): number {
  return Math.pow(10, db / 20)
}

export class ArcadeOverlayLayer {
  private ctx: AudioContext | null = null
  /** Overlay master bus——接到 ctx.destination（绕过主压缩器）*/
  private overlayBus: GainNode | null = null
  private kickBuf: AudioBuffer | null = null
  private hatBuf: AudioBuffer | null = null
  private clapBuf: AudioBuffer | null = null
  private swellBuf: AudioBuffer | null = null
  private pool: OverlayVoice[] = []
  private comboState: ComboState = createComboState()
  /** Swell 是持续节点（非每击触发）*/
  private swellSource: AudioBufferSourceNode | null = null
  private swellGain: GainNode | null = null
  /** 阶梯活跃状态，用于首次跨过阈值时的 fade-in */
  private activeLayers = { kick: false, clap: false, hat: false, swell: false }
  private callbacks: OverlayCallbacks = {}
  private activated = false

  async activate(ctx: AudioContext, callbacks: OverlayCallbacks = {}): Promise<void> {
    if (this.activated) return
    this.ctx = ctx
    this.callbacks = callbacks

    // Overlay master bus——绕过 AudioEngine 的 compressor，直连 destination
    this.overlayBus = ctx.createGain()
    this.overlayBus.gain.value = dbToGain(OVERLAY_MASTER_GAIN_DB)
    this.overlayBus.connect(ctx.destination)

    // 预分配 12 声部池
    for (let i = 0; i < OVERLAY_POOL_SIZE; i++) {
      const g = ctx.createGain()
      g.connect(this.overlayBus)
      this.pool.push({ gain: g, source: null, busy: false })
    }

    // 并行预解码所有采样——Mode 激活时一次性
    const [kick, hat, clap, swell] = await Promise.all([
      generateKick(),
      generateHat(),
      generateClap(),
      generateSwell(),
    ])
    this.kickBuf = kick
    this.hatBuf = hat
    this.clapBuf = clap
    this.swellBuf = swell

    this.comboState = createComboState()
    this.activated = true
    console.log('[Arcade] overlay activated, pool=', OVERLAY_POOL_SIZE)
  }

  deactivate(): void {
    if (!this.activated || !this.ctx) return
    // 停所有激活源
    for (const slot of this.pool) {
      if (slot.source) {
        try { slot.source.stop(); slot.source.disconnect() } catch {}
        slot.source = null
        slot.busy = false
      }
    }
    // 停 swell
    this.stopSwell(0)
    // 断 overlay bus
    if (this.overlayBus) {
      try { this.overlayBus.disconnect() } catch {}
      this.overlayBus = null
    }
    this.pool = []
    this.kickBuf = null
    this.hatBuf = null
    this.clapBuf = null
    this.swellBuf = null
    resetComboState(this.comboState)
    this.activeLayers = { kick: false, clap: false, hat: false, swell: false }
    this.activated = false
    console.log('[Arcade] overlay deactivated')
  }

  /**
   * 主 AudioEngine 在 playSound('down') 分支末尾调用。
   * playTime 是主音用的 ctx.currentTime + SCHEDULE_OFFSET——overlay 层共用同一 playTime 保证零感知延迟。
   */
  onKeydown(nowMs: number, playTime: number): void {
    if (!this.activated || !this.ctx) return
    const prevStage = getStage(this.comboState.combo)
    recordKeydown(this.comboState, nowMs)
    const stage = getStage(this.comboState.combo)

    this.triggerLayers(stage, playTime)

    if (stage !== prevStage) {
      this.callbacks.onStageChange?.(stage, this.comboState.combo)
    }

    if (checkPerfect(this.comboState, nowMs)) {
      this.callbacks.onPerfect?.()
    }
  }

  /**
   * Hold-repeat 事件入口——显式 no-op。AudioEngine 的 playSound('repeat')
   * 分支不调 onKeydown，不应该也调 onRepeat——但保留 API 便于未来扩展 /
   * assert 调用点正确性。
   */
  onRepeat(_nowMs: number): void {
    // no-op
  }

  /**
   * Combo 突然断开（比如用户暂停超过 reset 阈值）的通知入口。
   * 实际 combo reset 由 recordKeydown 内部 on next keydown 处理，这个函数主要用于
   * "用户切走 Rush Mode 但不 deactivate" 的场景（暂不支持）。保留骨架。
   */
  onReset(): void {
    resetComboState(this.comboState)
    this.stopAllPerPressLayers()
    this.stopSwell(SWELL_FADE_OUT_MS / 1000)
    this.activeLayers = { kick: false, clap: false, hat: false, swell: false }
    this.callbacks.onReset?.()
  }

  private triggerLayers(stage: ComboStage, playTime: number): void {
    if (!this.ctx) return
    const shouldHave = {
      kick:  stage !== 'idle',
      clap:  stage === 'stacking' || stage === 'flow' || stage === 'zone',
      hat:   stage === 'flow' || stage === 'zone',
      swell: stage === 'zone',
    }

    // Per-press 层（kick / clap / hat）
    if (shouldHave.kick && this.kickBuf)
      this.playVoice(this.kickBuf, playTime, LAYER_GAIN_DB.kick, LAYER_FADE_IN_MS.kick, 'kick')
    if (shouldHave.clap && this.clapBuf)
      this.playVoice(this.clapBuf, playTime, LAYER_GAIN_DB.clap, LAYER_FADE_IN_MS.clap, 'clap')
    if (shouldHave.hat && this.hatBuf)
      this.playVoice(this.hatBuf, playTime, LAYER_GAIN_DB.hat, LAYER_FADE_IN_MS.hat, 'hat')

    // Swell（非 per-press，只在 zone 进入时 fade-in，离开时 fade-out）
    if (shouldHave.swell && !this.activeLayers.swell) this.startSwell(playTime)
    if (!shouldHave.swell && this.activeLayers.swell) this.stopSwell(SWELL_FADE_OUT_MS / 1000)

    this.activeLayers = shouldHave
  }

  private playVoice(
    buf: AudioBuffer,
    playTime: number,
    gainDb: number,
    firstTimeFadeInMs: number,
    layer: 'kick' | 'clap' | 'hat',
  ): void {
    if (!this.ctx) return
    const slot = this.acquireSlot()
    const src = this.ctx.createBufferSource()
    src.buffer = buf

    const targetGain = dbToGain(gainDb)
    // 首次激活该层时 fade-in，后续直接 setValue
    const fadeIn = this.activeLayers[layer] ? 0 : firstTimeFadeInMs / 1000
    if (fadeIn > 0) {
      slot.gain.gain.setValueAtTime(0, playTime)
      slot.gain.gain.linearRampToValueAtTime(targetGain, playTime + fadeIn)
    } else {
      slot.gain.gain.setValueAtTime(targetGain, playTime)
    }

    src.connect(slot.gain)
    slot.source = src

    const dur = buf.duration
    // 尾部 5ms 斜降到 0 避免 click
    slot.gain.gain.setValueAtTime(targetGain, playTime + Math.max(0, dur - 0.005))
    slot.gain.gain.linearRampToValueAtTime(0, playTime + dur)

    src.start(playTime)
    src.stop(playTime + dur)
    src.onended = () => this.releaseSlot(slot)
  }

  private startSwell(playTime: number): void {
    if (!this.ctx || !this.swellBuf || !this.overlayBus) return
    const src = this.ctx.createBufferSource()
    src.buffer = this.swellBuf
    src.loop = true

    const gain = this.ctx.createGain()
    gain.gain.setValueAtTime(0, playTime)
    gain.gain.linearRampToValueAtTime(dbToGain(LAYER_GAIN_DB.swell), playTime + LAYER_FADE_IN_MS.swell / 1000)

    src.connect(gain)
    gain.connect(this.overlayBus)
    src.start(playTime)

    this.swellSource = src
    this.swellGain = gain
  }

  private stopSwell(fadeOutS: number): void {
    if (!this.ctx || !this.swellSource || !this.swellGain) return
    const now = this.ctx.currentTime
    const endTime = now + fadeOutS
    this.swellGain.gain.cancelScheduledValues(now)
    this.swellGain.gain.setValueAtTime(this.swellGain.gain.value, now)
    this.swellGain.gain.linearRampToValueAtTime(0, endTime)
    try { this.swellSource.stop(endTime + 0.01) } catch {}
    const s = this.swellSource
    const g = this.swellGain
    this.swellSource = null
    this.swellGain = null
    setTimeout(() => {
      try { s.disconnect() } catch {}
      try { g.disconnect() } catch {}
    }, fadeOutS * 1000 + 50)
  }

  private stopAllPerPressLayers(): void {
    if (!this.ctx) return
    const now = this.ctx.currentTime
    for (const slot of this.pool) {
      if (slot.source) {
        try {
          slot.gain.gain.cancelScheduledValues(now)
          slot.gain.gain.setValueAtTime(slot.gain.gain.value, now)
          slot.gain.gain.linearRampToValueAtTime(0, now + 0.008)
          slot.source.stop(now + 0.008)
        } catch {}
        slot.source = null
        slot.busy = false
      }
    }
  }

  private acquireSlot(): OverlayVoice {
    for (const s of this.pool) if (!s.busy) { s.busy = true; return s }
    // 全部忙：偷最老的那个（voice stealing）
    const stolen = this.pool[0]
    if (stolen.source && this.ctx) {
      const now = this.ctx.currentTime
      try {
        stolen.gain.gain.setValueAtTime(stolen.gain.gain.value, now)
        stolen.gain.gain.linearRampToValueAtTime(0, now + 0.008)
        stolen.source.stop(now + 0.008)
      } catch {}
    }
    stolen.busy = true
    return stolen
  }

  private releaseSlot(slot: OverlayVoice): void {
    if (slot.source) { try { slot.source.disconnect() } catch {} slot.source = null }
    slot.busy = false
  }
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `npm run build:renderer`
Expected: 无 TS 错误。

- [ ] **Step 3: Commit**

```bash
git add src/renderer/audio/arcade/ArcadeOverlayLayer.ts
git commit -m "feat(rush): ArcadeOverlayLayer · 12 声部独立池 + 4 层打击 + combo 集成"
```

---

### Task 6: 托盘 updateArcadeHud + IPC 管线

**Files:**
- Modify: `src/main/tray.ts`（新增 `updateArcadeHud` 导出 + 模板/非模板切换 + 脉冲 interval）
- Modify: `src/main/preload.ts`（新增 `updateArcadeHud` api）
- Modify: `src/main/ipc-handlers.ts`（注册 `update-arcade-hud` channel）

- [ ] **Step 1: 扩 `tray.ts` 加 `updateArcadeHud` 函数**

Edit `src/main/tray.ts`。在文件 top（import 后）加类型定义：

```ts
export type ArcadeHudState =
  | { kind: 'idle' }
  | { kind: 'active'; stage: 'engaged' | 'stacking' | 'flow' | 'zone'; perfect: boolean }
```

在文件 top 的模块级变量区加：

```ts
let defaultTemplateImg: Electron.NativeImage | null = null
let rushBaseImg: Electron.NativeImage | null = null
let rushBrightImg: Electron.NativeImage | null = null
let rushPerfectImg: Electron.NativeImage | null = null
let pulseTimer: ReturnType<typeof setInterval> | null = null
let perfectTimer: ReturnType<typeof setTimeout> | null = null
let pulseAlt = false
```

在 `createTrayIcon()` 旁边加一个辅助 loader：

```ts
function loadRushIcons(): void {
  if (rushBaseImg) return
  const scaleSuffix = ''  // Electron 自动根据 @2x 命名挑尺寸
  try {
    rushBaseImg = nativeImage.createFromPath(path.join(ROOT, 'assets', 'icons', 'tray-rush', `rush-base${scaleSuffix}.png`))
    rushBrightImg = nativeImage.createFromPath(path.join(ROOT, 'assets', 'icons', 'tray-rush', `rush-bright${scaleSuffix}.png`))
    rushPerfectImg = nativeImage.createFromPath(path.join(ROOT, 'assets', 'icons', 'tray-rush', `rush-perfect${scaleSuffix}.png`))
    // @2x 资源让 Electron 自动挑选——放在同目录的 @2x.png 即可
  } catch (err) {
    console.error('[Pekko] rush icons load failed:', err)
  }
}
```

修改 `createTrayIcon()` 让它**记住 default template image**：

```ts
function createTrayIcon(): Tray {
  const iconPath = path.join(ROOT, 'assets', 'icons', 'tray-icon.png')
  try {
    const img = nativeImage.createFromPath(iconPath)
    img.setTemplateImage(true)
    defaultTemplateImg = img  // 保存用于 reset
    return new Tray(img)
  } catch {
    return new Tray(nativeImage.createEmpty())
  }
}
```

在文件末加新 export：

```ts
export function updateArcadeHud(state: ArcadeHudState): void {
  if (!tray) return
  loadRushIcons()

  // PERFECT 闪 200ms，返回当前阶梯
  if (state.kind === 'active' && state.perfect && rushPerfectImg) {
    if (perfectTimer) clearTimeout(perfectTimer)
    tray.setImage(rushPerfectImg)
    perfectTimer = setTimeout(() => {
      applyBaseStage(state)
    }, 200)
    return
  }

  applyBaseStage(state)
}

function applyBaseStage(state: ArcadeHudState): void {
  if (!tray) return

  if (state.kind === 'idle') {
    // 回 template 模式
    if (pulseTimer) { clearInterval(pulseTimer); pulseTimer = null }
    if (defaultTemplateImg) tray.setImage(defaultTemplateImg)
    return
  }

  // Active：根据 stage 决定静态 vs 脉冲
  const { stage } = state
  if (stage === 'engaged' || stage === 'stacking') {
    // 静态 —— engaged 用 base，stacking 用 bright（饱和度更高）
    if (pulseTimer) { clearInterval(pulseTimer); pulseTimer = null }
    tray.setImage(stage === 'engaged' ? rushBaseImg! : rushBrightImg!)
    return
  }

  // flow / zone —— 2 帧交替脉冲
  const intervalMs = stage === 'flow' ? 500 : 250  // flow: 2s/cycle, zone: 1s/cycle
  if (pulseTimer) clearInterval(pulseTimer)
  pulseAlt = false
  pulseTimer = setInterval(() => {
    pulseAlt = !pulseAlt
    if (tray) tray.setImage(pulseAlt ? rushBrightImg! : rushBaseImg!)
  }, intervalMs)
  // 立即 set 一次，避免首帧等待
  tray.setImage(rushBaseImg!)
}
```

**注意**：`createTray` 现在声明了 `defaultTemplateImg`，如果 `loadRushIcons` 失败要有 fallback。为简化，失败时 active 态也用 default template image（退化到无可视反馈）。`updateArcadeHud` 里对 `rushBaseImg` 做 non-null 断言前加 null check：

```ts
function applyBaseStage(state: ArcadeHudState): void {
  if (!tray) return

  if (state.kind === 'idle' || !rushBaseImg || !rushBrightImg) {
    if (pulseTimer) { clearInterval(pulseTimer); pulseTimer = null }
    if (defaultTemplateImg) tray.setImage(defaultTemplateImg)
    return
  }
  // ... 余下同上
}
```

- [ ] **Step 2: 注册 IPC handler**

Edit `src/main/ipc-handlers.ts`。在 import 段加：

```ts
import { rebuildTrayMenu, updateArcadeHud, ArcadeHudState } from './tray'
```

在 `registerIpcHandlers` 函数末尾加 `ipcMain.on`（**不是** `ipcMain.handle`，因为 send/receive 是单向）：

```ts
ipcMain.on('update-arcade-hud', (_event, state: ArcadeHudState) => {
  // 不校验 state shape——preload 保证类型；如有恶意 payload 最多让 icon 回 idle
  updateArcadeHud(state)
})
```

- [ ] **Step 3: 扩 preload api**

Edit `src/main/preload.ts`。在 `contextBridge.exposeInMainWorld('api', { ... })` 里加一个 method（放在靠末尾）：

```ts
updateArcadeHud: (state: { kind: 'idle' } | { kind: 'active'; stage: 'engaged' | 'stacking' | 'flow' | 'zone'; perfect: boolean }) =>
  ipcRenderer.send('update-arcade-hud', state),
```

**重要**：这里用 `ipcRenderer.send`（fire-and-forget），不是 `ipcRenderer.invoke`——spec §6 明确要求，避免 IPC round-trip 阻塞音频线程。

对应的 TypeScript 类型声明（如果项目有 `window.api` 的 d.ts 定义），要同步扩加此 method；如果现有做法是隐式 `any`，可跳过。

- [ ] **Step 4: 验证编译**

```bash
npm run build:main
npm run build:renderer
```
Expected: 无错误。

- [ ] **Step 5: Commit**

```bash
git add src/main/tray.ts src/main/preload.ts src/main/ipc-handlers.ts
git commit -m "feat(rush): 托盘 updateArcadeHud + template/非 template 切换 + 脉冲 interval + IPC"
```

---

### Task 7: ArcadeHudController（渲染进程）

**Files:**
- Create: `src/renderer/audio/arcade/ArcadeHudController.ts`

渲染进程的 HUD 状态管理：接收 overlay 的 stage / perfect 事件，通过 rAF batch 节流后 IPC 给 main。

- [ ] **Step 1: 创建 ArcadeHudController.ts**

Create `src/renderer/audio/arcade/ArcadeHudController.ts`：

```ts
import { ComboStage } from './combo.ts'

type HudStage = 'engaged' | 'stacking' | 'flow' | 'zone'
type HudState =
  | { kind: 'idle' }
  | { kind: 'active'; stage: HudStage; perfect: boolean }

function stageToHud(stage: ComboStage): HudState {
  if (stage === 'idle') return { kind: 'idle' }
  return { kind: 'active', stage, perfect: false }
}

declare global {
  interface Window {
    api: {
      updateArcadeHud?: (state: HudState) => void
      [k: string]: unknown
    }
  }
}

export class ArcadeHudController {
  private activated = false
  private pending: HudState | null = null
  private lastSent: string = ''
  private rafHandle: number | null = null
  /** PERFECT 闪后 200ms 内新 stage 变化仍 rAF batch，正常节奏 */

  activate(): void {
    if (this.activated) return
    this.activated = true
    this.pushState({ kind: 'idle' })
  }

  deactivate(): void {
    if (!this.activated) return
    this.activated = false
    if (this.rafHandle !== null) {
      cancelAnimationFrame(this.rafHandle)
      this.rafHandle = null
    }
    this.pending = null
    // 立即 send idle——不走 rAF batch，确保 tray 马上回 template
    this.sendImmediate({ kind: 'idle' })
  }

  onStageChange(stage: ComboStage, _combo: number): void {
    if (!this.activated) return
    this.pushState(stageToHud(stage))
  }

  onPerfect(): void {
    if (!this.activated) return
    const base = this.pending ?? this.parseLastSent() ?? { kind: 'idle' }
    if (base.kind !== 'active') return
    this.pushState({ kind: 'active', stage: base.stage, perfect: true })
  }

  onReset(): void {
    if (!this.activated) return
    this.pushState({ kind: 'idle' })
  }

  private pushState(state: HudState): void {
    this.pending = state
    if (this.rafHandle !== null) return
    this.rafHandle = requestAnimationFrame(() => {
      this.rafHandle = null
      if (this.pending === null) return
      const key = JSON.stringify(this.pending)
      if (key !== this.lastSent) {
        this.sendImmediate(this.pending)
        this.lastSent = key
      }
      this.pending = null
    })
  }

  private sendImmediate(state: HudState): void {
    this.lastSent = JSON.stringify(state)
    window.api?.updateArcadeHud?.(state)
  }

  private parseLastSent(): HudState | null {
    if (!this.lastSent) return null
    try { return JSON.parse(this.lastSent) } catch { return null }
  }
}
```

- [ ] **Step 2: 验证编译**

Run: `npm run build:renderer`
Expected: 无 TS 错误。

- [ ] **Step 3: Commit**

```bash
git add src/renderer/audio/arcade/ArcadeHudController.ts
git commit -m "feat(rush): ArcadeHudController · rAF batch + stage/perfect IPC"
```

---

### Task 8: AudioEngine 集成

**Files:**
- Modify: `src/renderer/audio/AudioEngine.ts`

把 `ArcadeOverlayLayer` + `ArcadeHudController` 接入主引擎。在 `setMode` 时 activate/deactivate，在 `playSound('down')` 调 `onKeydown`，`playSound('repeat')` 不调，Rush Mode 下短路 adaptiveMultiplier。

- [ ] **Step 1: 在 AudioEngine.ts 加 import 和实例成员**

Edit `src/renderer/audio/AudioEngine.ts` 顶部 import 段，加：

```ts
import { ArcadeOverlayLayer } from './arcade/ArcadeOverlayLayer.ts'
import { ArcadeHudController } from './arcade/ArcadeHudController.ts'
```

在 `AudioEngine` 类中（靠其他实例成员处，如 `private activeVoices = 0` 之后）加：

```ts
private overlayLayer = new ArcadeOverlayLayer()
private hudController = new ArcadeHudController()
```

- [ ] **Step 2: 修改 setMode 让其处理 arcadeEnabled**

Edit `src/renderer/audio/AudioEngine.ts` 的 `setMode` 方法。当前实现只更新 DSP 参数，现在要加 activate/deactivate 逻辑：

```ts
setMode(modeOrId: string | Mode) {
  const mode = typeof modeOrId === 'string' ? getMode(modeOrId) : modeOrId
  const prevArcade = this.currentMode.arcadeEnabled
  this.currentMode = mode

  if (this.ctx && this.airLPF && this.highShelf && this.lowShelf && this.wetGain) {
    const now = this.ctx.currentTime
    const rampEnd = now + 0.3
    const rampParam = (p: AudioParam, target: number) => {
      p.cancelScheduledValues(now)
      p.setValueAtTime(p.value, now)
      p.linearRampToValueAtTime(target, rampEnd)
    }
    rampParam(this.airLPF.frequency, mode.style.airLpfHz)
    rampParam(this.highShelf.gain, mode.style.highShelfDb)
    rampParam(this.lowShelf.gain, mode.style.lowShelfDb)
    rampParam(this.wetGain.gain, mode.style.wetMix)
  }

  // Arcade 能力切换
  if (mode.arcadeEnabled && !prevArcade) {
    this.activateArcade()
  } else if (!mode.arcadeEnabled && prevArcade) {
    this.deactivateArcade()
  }

  console.log(`[Audio] mode=${mode.id} arcade=${mode.arcadeEnabled}`)
}

private async activateArcade(): Promise<void> {
  if (!this.ctx) return
  await this.overlayLayer.activate(this.ctx, {
    onStageChange: (stage, combo) => this.hudController.onStageChange(stage, combo),
    onPerfect:     () => this.hudController.onPerfect(),
    onReset:       () => this.hudController.onReset(),
  })
  this.hudController.activate()
}

private deactivateArcade(): void {
  this.overlayLayer.deactivate()
  this.hudController.deactivate()
}
```

**注意**：`mode.arcadeEnabled` 字段需要存在——Task 1 已加。若未加，这里 TS 会报错。

- [ ] **Step 3: 在 playSound('down') 末尾 hook overlay**

Edit `src/renderer/audio/AudioEngine.ts` 的 `playSound` 方法。在 `type === 'down'` 的分支（在主路径最后 `source.onended = ...` 之后），加 overlay hook。

找到方法末尾：

```ts
source.start(playTime, offsetJitter)
source.stop(playTime + dur)
source.onended = () => this.releaseSlot(slot)
```

紧接着加（还在 `playSound` 方法内）：

```ts
// === Arcade overlay hook ===
// 仅在 keydown（非 keyup）时驱动 overlay 和 combo。playTime 共享——和主音严格同调度。
if (type === 'down' && this.currentMode.arcadeEnabled) {
  const nowMs = performance.now()
  this.overlayLayer.onKeydown(nowMs, playTime)
}
```

**keyup 不触发 overlay**——overlay 只对 keydown 有反应（按下的瞬间才有 "打击感"），符合 drum sound 的物理惯例。

- [ ] **Step 4: Rush / arcade 模式下短路 adaptiveMultiplier**

Edit `src/renderer/audio/AudioEngine.ts` 的 `updateAdaptiveVolume` 方法：

```ts
private updateAdaptiveVolume() {
  const now = performance.now()

  // Rush / Custom+arcade 模式下完全砍疲劳衰减——由 spec §7 的 sprint 定位决定
  if (this.currentMode.arcadeEnabled) {
    this.adaptiveMultiplier = 1.0
    this.typingStartTime = 0
    this.lastAdaptiveUpdate = now
    return
  }

  // ... 现有逻辑不变
  if (now - this.lastKeyTime > 3000) {
    this.adaptiveMultiplier = 1.0
    this.typingStartTime = now
  } else if (this.typingStartTime === 0) {
    this.typingStartTime = now
  } else {
    const typingDuration = (now - this.typingStartTime) / 1000
    if (typingDuration < 5) {
      this.adaptiveMultiplier = 1.0
    } else if (typingDuration < 30) {
      this.adaptiveMultiplier = 1.0 - (typingDuration - 5) / 25 * 0.25
    } else {
      this.adaptiveMultiplier = Math.max(0.65, this.adaptiveMultiplier)
    }
  }

  this.lastAdaptiveUpdate = now
}
```

- [ ] **Step 5: 验证 'repeat' 路径不调 overlay**

再次 review `playSound` 方法的 `type === 'repeat'` 分支（现有代码 416 行起）——**确认该分支在触发自身逻辑后直接 `return`，不会 fall through 到底部的 overlay hook**。现有代码已经在 repeat 分支末尾 `return`，所以 overlay hook 自动被跳过。

在 repeat 分支末尾的 return 上方**加一个显式注释**（避免未来误改）：

```ts
// 现有 repeat 分支最后几行：
source.start(playTime)
source.stop(playTime + dur)
source.onended = () => this.releaseSlot(slot)
// Hold-repeat 不触发 overlay / combo——对齐 spec §4。该 return 必须保留，
// 否则会 fall through 到方法末尾的 overlay hook。
return
```

- [ ] **Step 6: destroy() 中清 overlay**

Edit `src/renderer/audio/AudioEngine.ts` 的 `destroy()` 方法，在现有逻辑开头加：

```ts
destroy() {
  // 先 deactivate arcade 能力（会清 overlay pool + swell + HUD）
  this.deactivateArcade()

  // ... 现有 destroy 逻辑
}
```

- [ ] **Step 7: 验证编译 + dev 运行**

```bash
npm run build:renderer && npm run build:main
```
Expected: 无 TS 错误。

```bash
npm run dev
```

Expected:
1. Pekko 启动，默认 Thock Mode——无 overlay、无 arcade log
2. 按 `›` 循环到 Rush Mode，console 出 `[Audio] mode=rush arcade=true` + `[Arcade] overlay activated, pool=12`
3. 托盘图标变成 warm amber（取决于 Task 6 的 PNG 是否正确加载）
4. 敲字幕 2~10 个字符，听到叠加的 kick；10+ 听到 clap；25+ 听到 hat；50+ 听到 shimmer pad
5. 停手 1 秒，浮现 fadeout；再敲，combo 重新从 1 开始
6. 开 hold-repeat（⇧⌘R），长按 ⌫ 5 秒——**不应**触发 overlay 堆叠，托盘图标**不应**跳阶梯

- [ ] **Step 8: Commit**

```bash
git add src/renderer/audio/AudioEngine.ts
git commit -m "feat(rush): AudioEngine 集成 · overlay hook + Rush 砍疲劳衰减 + mode 切换 activate"
```

---

### Task 9: Custom Mode UI · Arcade Toggle

**Files:**
- Modify: `src/renderer/components/TuneView.tsx`
- Modify: `src/renderer/hooks/useAudioEngine.ts`（如果 buildCustomMode 调用点要改）

- [ ] **Step 1: 理解现有 TuneView 结构**

Read `src/renderer/components/TuneView.tsx`——找到现有 bed / EQ / jitter 的 slider / toggle 模式，复制该模式加一个 "Arcade feedback" toggle。

通常模式：每一个 control 一个 `<div className="tune-row">` 或类似 wrapper，标签 + checkbox 或 slider。

- [ ] **Step 2: 找到 Custom 配置的 onChange 流程**

Grep `setCustomConfig` 在 TuneView 里的用法——它应该把 bed / bedGainDb / style 打包发给 main。我们要扩成也包含 `arcadeEnabled`。

Run: `grep -n "setCustomConfig\|customStyle" src/renderer/components/TuneView.tsx | head -20`

- [ ] **Step 3: 加 Arcade toggle state + JSX**

Edit `src/renderer/components/TuneView.tsx`。在现有 useState hooks 中加（靠 bed / style state 之后）：

```ts
const [arcadeEnabled, setArcadeEnabled] = useState<boolean>(
  initialSettings.customArcadeEnabled ?? false
)
```

其中 `initialSettings` 是 `useAudioEngine` 或父组件传下来的 settings——按现有组件签名调整。

在 onChange handler（调 `window.api.setCustomConfig`）中加字段：

```ts
const commit = () => {
  window.api.setCustomConfig({
    bed,
    bedGainDb,
    style,
    arcadeEnabled,  // NEW
  })
  // ... 现有后续通知 AudioEngine 的逻辑
}
```

在 JSX 中加一个 toggle 行，放在 jitter slider 之后（最末位置）：

```tsx
<div className="tune-row">
  <label>
    <input
      type="checkbox"
      checked={arcadeEnabled}
      onChange={(e) => {
        setArcadeEnabled(e.target.checked)
        // 切换后立即 commit——和其他 tune 行一致
      }}
    />
    Arcade feedback
  </label>
  <p className="tune-hint">Combo-driven overlay · tray pulse · built for sprints</p>
</div>
```

样式沿用现有 `.tune-row` / `.tune-hint` class。若现有 UI 用不同样式命名，对齐即可。

当 `arcadeEnabled` 在 Custom Mode 里变化，AudioEngine 也要被通知重建 Mode——通常是透传 settings 到 `useAudioEngine` hook，由它调 `audioEngine.setMode(buildCustomMode(...))`。

- [ ] **Step 4: 更新 buildCustomMode 调用点**

搜索所有 `buildCustomMode(` 调用，补第 4 个参数：

Run: `grep -rn "buildCustomMode(" src/`

预期调用点：`src/renderer/hooks/useAudioEngine.ts`（或者 `useProfiles.ts`）。典型老代码：

```ts
const mode = buildCustomMode(settings.customStyle, settings.customBed, settings.customBedGainDb)
```

改为：

```ts
const mode = buildCustomMode(
  settings.customStyle,
  settings.customBed,
  settings.customBedGainDb,
  settings.customArcadeEnabled,
)
```

- [ ] **Step 5: dev 验证**

```bash
npm run dev
```

Expected:
1. 切到 Custom Mode，打开 Tune 面板
2. 看到 "Arcade feedback" toggle 行，默认 unchecked
3. Tick 打开 —— 控制台出 `[Audio] mode=custom arcade=true` + `[Arcade] overlay activated`
4. 敲字，听到和 Rush Mode 一致的 overlay 堆叠
5. 重启 Pekko（退出 → 再开），toggle 状态**持久化**，上次开的仍开
6. Un-tick —— 控制台出 `[Arcade] overlay deactivated`，托盘图标回 template

- [ ] **Step 6: Commit**

```bash
git add src/renderer/components/TuneView.tsx src/renderer/hooks/
git commit -m "feat(rush): Custom Mode · Arcade feedback toggle · opt-in overlay + HUD"
```

---

### Task 10: 端到端验收

**Files:** N/A（纯手动测试）

运行 spec §13 的 11 条验收标准。每条走一遍，记录结果。

- [ ] **Step 1: 逐条跑 spec §13 验收清单**

```bash
npm run dev
```

对照 `docs/superpowers/specs/2026-04-15-rush-mode-design.md` §13：

1. ☐ 切到 Rush Mode，连续打字 10+ 键：听感上叠加 kick 清晰可辨
2. ☐ 连续打字 25+ 键：clap 加入，音色变厚
3. ☐ 连续打字 50+ 键：能听到 shimmer pad 铺底
4. ☐ 停手 600ms+：所有 overlay 层干净退出，无残留
5. ☐ 单键延迟测量：`console.log` 显示 `schedOffset=2ms`，主观听感无感知延迟变化
6. ☐ 切到 Classic Mech / Thock：托盘图标恢复默认 template，overlay pool 完全释放（打开 DevTools → Memory → Heap Snapshot 确认 `ArcadeOverlayLayer` instance 没有持有的 `AudioBufferSourceNode`）
7. ☐ Custom Mode 开启 `customArcadeEnabled`：overlay + HUD 行为与 Rush Mode 一致
8. ☐ 连续打字 5 分钟：无 voice starvation（console 无 "steal" warning）、无 compressor pump（主音色不变弱）、无 click artifacts
9. ☐ 托盘图标在 macOS **light + dark 菜单栏下**：切 System Preferences → Appearance，检查 warm amber PNG 在两种背景下都清晰可辨
10. ☐ Rush ↔ 其他 Mode 切换：10 次连续切换，托盘图标正确切换 template/非 template，无残留帧、无图标消失
11. ☐ 长按 ⌫ 持续 5 秒（⇧⌘R 开启 hold-repeat）：combo 无变化、overlay 层无触发，只听主路径 repeat-path 轴体音

每一条都打勾 / 记问题。

- [ ] **Step 2: 修复发现的问题**

按重要性排序，针对发现的 bug 逐一修复。每修完一个 commit 一次。

- [ ] **Step 3: 最终 commit（如果还有未提交的修复）**

```bash
git add -A
git commit -m "fix(rush): 验收修复（具体问题描述）"
```

- [ ] **Step 4: 推分支（可选，等用户确认）**

```bash
git push -u origin worktree-lovely-sparking-fern
```

实际分支 / PR 策略按用户决定——有可能要先 merge 到 `design/chassis-v2`。

---

## 附：回查清单

- [ ] `AppSettings.customArcadeEnabled` 正确持久化（修改 → 重启 → 恢复）
- [ ] `Mode.arcadeEnabled` 在 4 个 preset 上都是 `false`，只有 Rush 是 `true`
- [ ] `STYLE.rush` 所有字段在 `CUSTOM_RANGE` 内（Custom 用户可复现 Rush 的 DSP）
- [ ] `playSound('repeat')` 分支结束有 `return` 阻止 fall-through 到 overlay hook
- [ ] Overlay bus 绕过主 compressor（不连 compressor，直连 destination）
- [ ] Overlay pool size = 12
- [ ] Combo reset interval = 600ms
- [ ] PERFECT 阈值 = σ/μ < 0.15
- [ ] PERFECT 冷却 = 2s
- [ ] Stage thresholds = 2 / 10 / 25 / 50
- [ ] 托盘图标 idle 状态是 template，active 状态是非 template
- [ ] 托盘 PNG 文件均存在（base / bright / perfect × 2 sizes = 6 PNG）

如 plan 执行中某一步 blocker 无法解决，回到此 plan 的相关 task，看它依赖什么上游决策——如果涉及 spec 里的开放项（spec §12），反馈给 pochita 确认。
